import React from 'react';
import * as Styled from './styled';
import Logo from '../../Logo/Logo';

const toolbar = () => (
  <Styled.Toolbar>
    <div>MENU</div>
    <Logo/>
    <nav>
      ...
    </nav>
  </Styled.Toolbar>
);

export default toolbar;