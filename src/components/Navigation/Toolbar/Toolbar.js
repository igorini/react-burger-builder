import React from 'react';
import * as Styled from './styled';

const toolbar = props => (
  <Styled.Toolbar>
    <div>MENU</div>
    <div>LOGO</div>
    <nav>
      ...
    </nav>
  </Styled.Toolbar>
);

export default toolbar;